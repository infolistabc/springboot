server:
  port: 8001
#redis连接配置
spring:
  redis:
    host: 127.0.0.1
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: 8
        min-idle: 8
      shutdown-timeout: 100